/**
 * Application-wide constants
 */

// Pagination
export const DEFAULT_PAGE_SIZE = 10;
export const MAX_PAGE_SIZE = 100;

// Business Rules
export const MAX_CUSTOMERS_PER_USER = 5;

// JWT
export const JWT_EXPIRATION = '7d';

// Bcrypt
export const BCRYPT_SALT_ROUNDS = 10;
